{
	"name": "whatchar",
	"displayName": "whatchar",
	"description": "what character does it points at",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.46.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "whatchar.show",
				"title": "Show what character"
			}
		],
		"configuration": {
			"title": "whatchar",
			"properties": {
				"whatchar.showOnCursorMove": {
					"type": "boolean",
					"default": true,
					"description": "Show character info in the status bar whenever the cursor moves."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "yarn run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./"
	},
	"dependencies": {
		"iconv-lite": "^0.6.3"
	},
	"devDependencies": {
		"@types/node": "^13.11.0",
		"@types/vscode": "^1.46.0",
		"typescript": "^3.8.3"
	}
}
